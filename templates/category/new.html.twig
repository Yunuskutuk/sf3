{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajout categori</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type='submit' value='Add category'>
    {{ form_end(form) }}
{% endblock %}